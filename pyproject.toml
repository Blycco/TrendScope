[tool.ruff]
target-version = "py311"
line-length = 100
src = ["backend", "tests"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
    "S",   # flake8-bandit (security)
    "ANN", # flake8-annotations (type hints)
]
ignore = [
    "S101",    # use of assert (ok in tests)
    "UP017",   # timezone.utc preferred over datetime.UTC (Python 3.9 dev compat)
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101", "ANN"]
"backend/crawler/sources/rss_feeds.py" = ["E501"]  # URL constants cannot be line-wrapped

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "--cov=backend --cov-report=term-missing --cov-fail-under=70"

[tool.coverage.run]
source = ["backend"]
omit = ["backend/*/__init__.py"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]
